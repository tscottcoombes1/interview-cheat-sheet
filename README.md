Setup:

```


Setting up a new workspace:

```
uv init <workspace>
```

Basic examples for:

* pandas
* polars
* pyarrow

for extracting data.


Running a notebook with uv

uv run --with jupyter jupyter lab
